# Syntecxhub-encrypted-chat-app
A simple client/server chat application in Python with AES-CBC encryption — TCP sockets, pre-shared key encryption, random IV per message, multi-client support via threading, and server-side message logging.
